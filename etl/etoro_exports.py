# Placeholder: parser per export eToro (XLS/CSV) -> parquet
# In seguito implementeremo:
# - parse_trades(filepath) -> DataFrame
# - parse_positions(filepath) -> DataFrame
# - normalizzazione colonne / timezone
